# 📊 UML Diagram - Hệ thống Đăng Ký Tín Chỉ

## 1️⃣ Class Diagram - Model Layer

```
┌─────────────────────────────────┐
│           <<abstract>>          │
│            User                 │
├─────────────────────────────────┤
│ - userId: String                │
│ - username: String              │
│ - email: String                 │
│ - password: String              │
│ - fullName: String              │
│ - role: String                  │
│ - createdAt: LocalDateTime      │
├─────────────────────────────────┤
│ + login(): boolean              │
│ + logout(): void                │
│ + changePassword(): boolean     │
│ + getDetails(): String          │
└──────────────┬──────────────────┘
               △
               │
        ┌──────┴──────┐
        │             │
┌───────┴────────┐  ┌──┴──────────────┐
│   Student      │  │      Admin      │
├────────────────┤  ├─────────────────┤
│- studentId     │  │- adminId        │
│- className     │  │- department     │
│- major: Major  │  │- role: String   │
│- faculty: Fac. │  └─────────────────┘
├────────────────┤
│+ getSchedule() │
│+ getGPA()      │
└────────────────┘
```

### Model Classes Detail

#### User (Abstract Base Class)
```
┌────────────────────────────────────┐
│        User                        │
├────────────────────────────────────┤
│ userId: String (PK)                │
│ username: String (Unique)          │
│ email: String (Unique)             │
│ password: String (Hashed)          │
│ fullName: String                   │
│ role: String (ENUM)                │
│ createdAt: LocalDateTime           │
│ updatedAt: LocalDateTime           │
├────────────────────────────────────┤
│ + register(): User                 │
│ + login(): boolean                 │
│ + logout(): void                   │
│ + changePassword(): boolean        │
│ + updateProfile(): boolean         │
│ + getDetails(): String             │
│ + validatePassword(): boolean      │
└────────────────────────────────────┘
        △ (Inheritance)
        │
   ┌────┴────┐
   │         │
Student     Admin
```

#### Student Class
```
┌──────────────────────────────────┐
│        Student                   │
├──────────────────────────────────┤
│ studentId: String (PK)           │
│ userId: String (FK→User)         │
│ className: String                │
│ majorId: String (FK→Major)       │
│ facultyId: String (FK→Faculty)   │
│ enrollmentDate: LocalDate        │
├──────────────────────────────────┤
│ + getMyRegistrations()           │
│ + getSchedule()                  │
│ + getTotalCredits()              │
│ + getGPA()                       │
│ + updateProfile()                │
│ + getAvailableCourses()          │
└──────────────────────────────────┘
     Associations:
     - 1 Student → * Registration
     - 1 Student → 1 Major
     - 1 Student → 1 Faculty
```

#### Admin Class
```
┌──────────────────────────────────┐
│        Admin                     │
├──────────────────────────────────┤
│ adminId: String (PK)             │
│ userId: String (FK→User)         │
│ department: String               │
│ permissions: String              │
│ startDate: LocalDate             │
├──────────────────────────────────┤
│ + manageUsers()                  │
│ + manageCoursesOffering()        │
│ + manageRegistrations()          │
│ + generateReports()              │
│ + manageFaculty()                │
│ + manageSchedules()              │
└──────────────────────────────────┘
```

#### Course Class
```
┌──────────────────────────────────┐
│        Course                    │
├──────────────────────────────────┤
│ courseId: String (PK)            │
│ courseName: String               │
│ courseCode: String               │
│ credits: Integer                 │
│ description: String              │
│ facultyId: String (FK→Faculty)   │
│ createdAt: LocalDate             │
├──────────────────────────────────┤
│ + getDetails()                   │
│ + updateInfo()                   │
│ + getCourseOfferings()           │
│ + validate()                     │
└──────────────────────────────────┘
     Associations:
     - 1 Course → * CourseOffering
     - 1 Faculty → * Course
```

#### CourseOffering Class
```
┌───────────────────────────────────┐
│     CourseOffering               │
├───────────────────────────────────┤
│ courseOfferingId: String (PK)    │
│ courseId: String (FK→Course)     │
│ semesterId: String (FK→Semester) │
│ facultyId: String (FK→Faculty)   │
│ maxCapacity: Integer             │
│ currentCapacity: Integer         │
│ startDate: LocalDate             │
│ endDate: LocalDate               │
├───────────────────────────────────┤
│ + checkScheduleConflict()        │
│ + hasAvailableSlot()             │
│ + getSchedules()                 │
│ + getRegisteredStudents()        │
│ + incrementCapacity()            │
│ + decrementCapacity()            │
└───────────────────────────────────┘
     Associations:
     - 1 Course → * CourseOffering
     - 1 Semester → * CourseOffering
     - 1 Faculty → * CourseOffering
     - 1 Room → * CourseOffering
     - * CourseOffering → * Schedule (via CourseOfferingSchedule)
     - 1 CourseOffering → * Registration
```

#### Registration Class
```
┌──────────────────────────────────┐
│      Registration                │
├──────────────────────────────────┤
│ registrationId: String (PK)      │
│ studentId: String (FK→Student)   │
│ courseOfferingId: String (FK)    │
│ registrationDate: LocalDateTime  │
│ status: String (ENUM)            │
│ notes: String                    │
│ grade: Double                    │
├──────────────────────────────────┤
│ + updateStatus()                 │
│ + assignGrade()                  │
│ + getDetails()                   │
│ + validate()                     │
└──────────────────────────────────┘
     Status Values:
     - "SUCCESS" (Đăng ký thành công)
     - "FAILED" (Đăng ký thất bại)
     - "CANCELLED" (Đã hủy)
     
     Associations:
     - * Student → 1 CourseOffering (via Registration)
```

#### Schedule Class
```
┌───────────────────────────────────┐
│        Schedule                  │
├───────────────────────────────────┤
│ scheduleId: String (PK)          │
│ dayOfWeek: Integer (2-8)         │
│ startTime: LocalTime             │
│ endTime: LocalTime               │
│ dayName: String (T2-CN)          │
│ createdAt: LocalDateTime         │
├───────────────────────────────────┤
│ + getDayName()                   │
│ + getTimeDuration()              │
│ + hasConflict()                  │
│ + validate()                     │
└───────────────────────────────────┘
     Day of Week Values:
     2 = T2 (Monday)
     3 = T3 (Tuesday)
     4 = T4 (Wednesday)
     5 = T5 (Thursday)
     6 = T6 (Friday)
     7 = T7 (Saturday)
     8 = CN (Sunday)
     
     Associations:
     - * Schedule ← → * CourseOffering (via CourseOfferingSchedule)
```

#### CourseOfferingSchedule Class
```
┌──────────────────────────────────┐
│  CourseOfferingSchedule          │
├──────────────────────────────────┤
│ id: Integer (PK)                 │
│ courseOfferingId: String (FK)    │
│ scheduleId: String (FK)          │
│ startDate: LocalDate             │
│ endDate: LocalDate               │
│ createdAt: LocalDateTime         │
├──────────────────────────────────┤
│ + getDetails()                   │
│ + updateDates()                  │
│ + isActive()                     │
└──────────────────────────────────┘
     Join Table:
     Links CourseOffering ↔ Schedule
```

#### Semester Class
```
┌──────────────────────────────────┐
│       Semester                   │
├──────────────────────────────────┤
│ semesterId: String (PK)          │
│ semesterName: String             │
│ academicYear: String             │
│ startDate: LocalDate             │
│ endDate: LocalDate               │
│ registrationStart: LocalDate     │
│ registrationEnd: LocalDate       │
│ registrationStartTime: LocalTime │
│ registrationEndTime: LocalTime   │
│ status: String (OPEN/CLOSED)     │
├──────────────────────────────────┤
│ + isOpen()                       │
│ + isCurrentSemester()            │
│ + isRegistrationOpen()           │
│ + updateDates()                  │
└──────────────────────────────────┘
     Associations:
     - 1 Semester → * CourseOffering
     - 1 Semester → * Registration
```

#### Room Class
```
┌──────────────────────────────────┐
│         Room                     │
├──────────────────────────────────┤
│ roomId: String (PK)              │
│ roomName: String                 │
│ building: String                 │
│ capacity: Integer                │
│ hasProjector: boolean            │
│ hasAC: boolean                   │
│ createdAt: LocalDateTime         │
├──────────────────────────────────┤
│ + getAvailableSlots()            │
│ + getSchedules()                 │
│ + hasEquipment()                 │
│ + validate()                     │
└──────────────────────────────────┘
     Associations:
     - 1 Room → * CourseOffering
```

#### Faculty Class
```
┌──────────────────────────────────┐
│        Faculty                   │
├──────────────────────────────────┤
│ facultyId: String (PK)           │
│ facultyName: String              │
│ description: String              │
│ headOfDepartment: String         │
│ createdAt: LocalDateTime         │
├──────────────────────────────────┤
│ + getDetails()                   │
│ + getCoursesCount()              │
│ + updateInfo()                   │
└──────────────────────────────────┘
     Associations:
     - 1 Faculty → * Course
     - 1 Faculty → * Major
     - 1 Faculty → * User (Admin)
```

#### Major Class
```
┌──────────────────────────────────┐
│         Major                    │
├──────────────────────────────────┤
│ majorId: String (PK)             │
│ majorName: String                │
│ facultyId: String (FK→Faculty)   │
│ description: String              │
│ requiredCredits: Integer         │
│ createdAt: LocalDateTime         │
├──────────────────────────────────┤
│ + getStudents()                  │
│ + updateInfo()                   │
│ + getRequiredCourses()           │
└──────────────────────────────────┘
     Associations:
     - 1 Faculty → * Major
     - 1 Major → * Student
```

---

## 2️⃣ Class Diagram - Repository Layer

```
┌─────────────────────────────────┐
│   <<interface>>                 │
│   BaseRepository<T>             │
├─────────────────────────────────┤
│ + insert(T)                     │
│ + update(T)                     │
│ + delete(id)                    │
│ + findById(id)                  │
│ + findAll()                     │
│ + exists(id)                    │
└─────────────────────────────────┘
           △
           │ implements
        ┌──┴──┬───────────────┬──────────┐
        │     │               │          │
    UserRepo  CourseRepo  RegistrationRepo...
```

### Repository Classes

```
┌────────────────────────────────────┐
│    UserRepository                  │
├────────────────────────────────────┤
│ - connection: Connection           │
├────────────────────────────────────┤
│ + insert(User): void               │
│ + findById(userId): User           │
│ + findByUsername(username): User   │
│ + findByEmail(email): User         │
│ + findAll(): List<User>            │
│ + update(User): void               │
│ + delete(userId): void             │
│ + exists(userId): boolean          │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│   CourseOfferingRepository         │
├────────────────────────────────────┤
│ + insert(CourseOffering): void     │
│ + findById(id): CourseOffering     │
│ + findByCourse(courseId): List     │
│ + findBySemester(semesterId): List │
│ ⭐ + checkScheduleConflict()       │
│    (roomId, semesterId,            │
│     dayOfWeek, startTime,          │
│     endTime): boolean              │
│ + findAvailable(): List            │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│  RegistrationRepository            │
├────────────────────────────────────┤
│ + insert(Registration): void       │
│ + findById(id): Registration       │
│ + findByStudent(studentId): List   │
│ + findByCourseOffering(id): List   │
│ + findByStatus(status): List       │
│ + update(Registration): void       │
│ + delete(id): void                 │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│    ScheduleRepository              │
├────────────────────────────────────┤
│ + insert(Schedule): void           │
│ + findById(id): Schedule           │
│ + findByDay(dayOfWeek): List       │
│ + findByCourseOffering(id): List   │
│ + update(Schedule): void           │
│ + delete(id): void                 │
└────────────────────────────────────┘
```

---

## 3️⃣ Class Diagram - Service Layer

```
┌──────────────────────────────────┐
│      <<interface>>               │
│     UserService                  │
├──────────────────────────────────┤
│ + register(User): User           │
│ + login(username): User          │
│ + getUserById(id): User          │
│ + updateUser(User): boolean      │
│ + changePassword(): boolean      │
│ + deleteUser(id): boolean        │
│ + validateUser(): boolean        │
└──────────────────────────────────┘
           △
           │ extends
        ┌──┴───────────────┐
        │                  │
    AdminService    StudentService
```

### Service Interfaces & Implementations

```
┌────────────────────────────────────┐
│    <<interface>>                   │
│     AuthService                    │
├────────────────────────────────────┤
│ + login(username, pwd): User       │
│ + logout(): void                   │
│ + getCurrentUser(): User           │
│ + isAuthenticated(): boolean       │
│ + changePassword(): boolean        │
│ + isSessionValid(): boolean        │
└────────────────────────────────────┘
           △
           │ implements
           │
┌────────────────────────────────────┐
│     AuthServiceImpl                 │
├────────────────────────────────────┤
│ - userRepository: UserRepository   │
│ - currentUser: User                │
├────────────────────────────────────┤
│ + login(username, pwd): User       │
│ + logout(): void                   │
│ + getCurrentUser(): User           │
│ + verifyPassword(): boolean        │
│ + hashPassword(): String           │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│    <<interface>>                   │
│   CourseOfferingService            │
├────────────────────────────────────┤
│ + createCourseOffering(): CO       │
│ + getCourseOfferingById(): CO      │
│ + getAllCourseOfferings(): List    │
│ ⭐ + validate schedule conflict    │
│ + updateCourseOffering(): boolean  │
│ + deleteCourseOffering(): boolean  │
└────────────────────────────────────┘
           △
           │ implements
           │
┌────────────────────────────────────┐
│  CourseOfferingServiceImpl          │
├────────────────────────────────────┤
│ - repository: CORepository         │
│ - scheduleService: ScheduleService │
├────────────────────────────────────┤
│ + createCourseOffering()           │
│   ⭐ FOR EACH schedule:            │
│      IF checkScheduleConflict()    │
│        THROW IllegalArgumentException
│      ELSE create CourseOffering    │
│ + updateCourseOffering()           │
│ + validateSchedules()              │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│    <<interface>>                   │
│  RegistrationService               │
├────────────────────────────────────┤
│ + registerCourse(): Registration   │
│ + cancelRegistration(): boolean    │
│ ⭐ + check duplicate registration │
│ + getRegistrationsByStudent(): L   │
│ + updateStatus(): boolean          │
└────────────────────────────────────┘
           △
           │ implements
           │
┌────────────────────────────────────┐
│   RegistrationServiceImpl           │
├────────────────────────────────────┤
│ - repository: RegistrationRepo     │
│ - courseOfferingService: COService │
├────────────────────────────────────┤
│ + registerCourse()                 │
│   ⭐ Check duplicate:              │
│      IF isDuplicateRegistration()  │
│        THROW IllegalArgumentException
│      ELSE create registration      │
│ + validateRegistration()           │
│ + checkCapacity()                  │
└────────────────────────────────────┘
```

---

## 4️⃣ Class Diagram - Controller Layer

```
┌────────────────────────────────┐
│     LoginController            │
├────────────────────────────────┤
│ - authService: AuthService     │
├────────────────────────────────┤
│ + handleLogin(): void          │
│ + validateInput(): boolean     │
│ + showErrorMessage(): void     │
└────────────────────────────────┘
        │ uses
        ↓
┌────────────────────────────────┐
│    DashboardController         │
├────────────────────────────────┤
│ - currentUser: User            │
│ - navigationManager: NavMgr    │
├────────────────────────────────┤
│ + loadDashboard(): void        │
│ + navigateTo(screen): void     │
│ + logout(): void               │
└────────────────────────────────┘
        │ uses
        ├─→ StudentController
        ├─→ AdminController
        ├─→ CourseOfferingController
        ├─→ RegistrationController
        └─→ ScheduleController

┌──────────────────────────────────────┐
│  CourseOfferingController            │
├──────────────────────────────────────┤
│ - courseOfferingService              │
│ - scheduleService                    │
│ - roomService                        │
├──────────────────────────────────────┤
│ + handleCreate(): void               │
│ + handleEdit(): void                 │
│ + handleDelete(): void               │
│ + handleScheduleConflict(): void     │
│ + showErrorDialog(): void            │
│ + validateForm(): boolean            │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│CreateCourseOfferingFormController   │
├──────────────────────────────────────┤
│ - courseOfferingService              │
│ - scheduleService                    │
│ - chosenSchedules: List<ScheduleRow> │
├──────────────────────────────────────┤
│ + handleSave(): void                 │
│   ⭐ Load schedules from DB:         │
│      FOR EACH scheduleId:            │
│        schedule = scheduleService    │
│                  .getScheduleById()  │
│      offering.setSchedules(list)     │
│      service.createCourseOffering()  │
│ + buildCourseOffering(): CO          │
│ + validateForm(): boolean            │
└──────────────────────────────────────┘

┌────────────────────────────────┐
│  RegistrationController        │
├────────────────────────────────┤
│ - registrationService          │
│ - studentService               │
├────────────────────────────────┤
│ + handleRegister(): void       │
│ ⭐ Check duplicate:            │
│   + isDuplicate(): boolean     │
│ + handleCancel(): void         │
│ + loadMyRegistrations(): void  │
└────────────────────────────────┘
```

---

## 5️⃣ Sequence Diagram - Student Registration Flow

```
Student         UI              Controller      Service         Repository      Database
  │             │                   │              │                │              │
  │─ Click "Đăng Ký"───────────────→│              │                │              │
  │             │                   │              │                │              │
  │             │          handleRegister()       │                │              │
  │             │                   │── checkDuplicate──────────────→│              │
  │             │                   │              │                │  Query users  │
  │             │                   │              │                │─────────────→│
  │             │                   │              │                │  (Check exists)
  │             │                   │              │                │←─────────────│
  │             │                   │← OK ────────│                │              │
  │             │                   │              │                │              │
  │             │          registerCourse()       │                │              │
  │             │                   │──create Registration─────────→│              │
  │             │                   │              │                │  INSERT into │
  │             │                   │              │                │  registrations
  │             │                   │              │                │─────────────→│
  │             │                   │              │                │    Success   │
  │             │                   │              │                │←─────────────│
  │             │                   │← Success ───│                │              │
  │             │← Show Success ─────│              │                │              │
  │← Success ────│                   │              │                │              │
```

---

## 6️⃣ Sequence Diagram - Schedule Conflict Detection

```
Admin           UI              Controller      Service         Repository      Database
  │             │                   │              │                │              │
  │─ Create Class─────────────────→│              │                │              │
  │   with schedules              │              │                │              │
  │             │                   │              │                │              │
  │             │        handleSave()             │                │              │
  │             │                   │─ createCourseOffering─────────→│              │
  │             │                   │              │                │              │
  │             │                   │  FOR EACH schedule:           │              │
  │             │                   │      checkScheduleConflict()  │              │
  │             │                   │              │────────────────→│              │
  │             │                   │              │                │  SELECT join │
  │             │                   │              │                │  (Check room)
  │             │                   │              │                │─────────────→│
  │             │                   │              │                │  same room   │
  │             │                   │              │                │  same day    │
  │             │                   │              │                │  time overlap
  │             │                   │              │                │←─────────────│
  │             │                   │              │    ← Conflict──│              │
  │             │                   │ ←─ Exception─│                │              │
  │             │← Error Dialog ────│              │                │              │
  │← Conflict ───│                   │              │                │              │
  │  Message     │                   │              │                │              │

[Success Case - No Conflict]
  │             │                   │              │                │              │
  │             │                   │              │    ← No conflict
  │             │                   │              │                │              │
  │             │                   │      CREATE course_offering   │              │
  │             │                   │              │────────────────→│              │
  │             │                   │              │                │  INSERT      │
  │             │                   │              │                │─────────────→│
  │             │                   │              │                │    Success   │
  │             │                   │              │                │←─────────────│
  │             │← Success ─────────│              │                │              │
  │← Class ─────│                   │              │                │              │
  │  Created    │                   │              │                │              │
```

---

## 7️⃣ State Diagram - Registration Status

```
                    ┌─────────────┐
                    │   PENDING   │
                    │ (Created)   │
                    └──────┬──────┘
                           │
                    ┌──────┴──────┐
                    │             │
                    ▼             ▼
            ┌─────────────┐ ┌──────────────┐
            │  SUCCESS    │ │    FAILED    │
            │(Registered)│ │(Duplicate/   │
            └──────┬──────┘ │ Full/Conflict)
                   │        └──────┬───────┘
                   │               │
                   │        ┌──────┴──────┐
                   │        │             │
                   ▼        ▼             │
            ┌─────────────────────────┐  │
            │    CANCELLED            │  │
            │ (Student withdraw)      │  │
            └─────────────────────────┘  │
                                         │
                   ┌─────────────────────┘
                   │
                   ▼
            ┌─────────────┐
            │  REJECTED   │
            │(By Admin)   │
            └─────────────┘
```

---

## 8️⃣ Package Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                   java-oop-ptit                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐   │
│  │   config     │   │    model     │   │ repository   │   │
│  │              │   │              │   │              │   │
│  │ Database     │   │ • User       │   │ • UserRepo   │   │
│  │ Connection   │   │ • Student    │   │ • CourseRepo │   │
│  │              │   │ • Admin      │   │ • RegisRepo  │   │
│  │              │   │ • Course     │   │ • ScheduleR  │   │
│  │              │   │ • CourseOff  │   │              │   │
│  │              │   │ • Registrat  │   │              │   │
│  │              │   │ • Schedule   │   │              │   │
│  │              │   │ • Semester   │   │              │   │
│  │              │   │ • Room       │   │              │   │
│  │              │   │ • Faculty    │   │              │   │
│  │              │   │ • Major      │   │              │   │
│  └──────────────┘   └──────────────┘   └──────────────┘   │
│         │                  △                    △          │
│         └──────────────────┼────────────────────┘          │
│                            │                               │
│                    ┌───────┴────────┐                      │
│                    │                │                      │
│  ┌─────────────────┴────┐  ┌────────┴──────────────┐     │
│  │     service      │  │    controller          │     │
│  │                  │  │                        │     │
│  │ • AuthService    │  │ • LoginCtrl            │     │
│  │ • UserService    │  │ • DashboardCtrl        │     │
│  │ • AdminService   │  │ • StudentCtrl          │     │
│  │ • StudentSvc     │  │ • AdminCtrl            │     │
│  │ • CourseSvc      │  │ • CourseOfferingCtrl   │     │
│  │ • CourseOffSvc   │  │ • RegistrationCtrl     │     │
│  │ • RegistrationSv │  │ • ScheduleCtrl         │     │
│  │ • ScheduleService│  │                        │     │
│  └──────────────────┘  └────────────────────────┘     │
│         △                           △                   │
│         └───────────────┬───────────┘                   │
│                         │                               │
│                ┌────────┴────────┐                      │
│                │                 │                      │
│  ┌─────────────┴──────┐   ┌──────┴────────────┐        │
│  │      view          │   │    utils          │        │
│  │                    │   │                   │        │
│  │ • AppView          │   │ • GenericUtils    │        │
│  │ • NavigationMgr    │   │ • FXUtils         │        │
│  │                    │   │ • ValidationUtils │        │
│  │                    │   │                   │        │
│  └────────────────────┘   └───────────────────┘        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 9️⃣ Component Diagram

```
┌─────────────────────────────────────────────┐
│          User Interface Layer               │
│  ┌──────────────────────────────────────┐   │
│  │  JavaFX View                         │   │
│  │  • Login.fxml                        │   │
│  │  • StudentDashboard.fxml             │   │
│  │  • AdminDashboard.fxml               │   │
│  │  • style.css                         │   │
│  └──────────────────────────────────────┘   │
└──────────────┬──────────────────────────────┘
               │ uses
               ▼
┌─────────────────────────────────────────────┐
│       Presentation Layer                    │
│  ┌──────────────────────────────────────┐   │
│  │  Controllers                         │   │
│  │  • LoginController                   │   │
│  │  • StudentController                 │   │
│  │  • AdminController                   │   │
│  │  • CourseOfferingController          │   │
│  │  • RegistrationController            │   │
│  └──────────────────────────────────────┘   │
└──────────────┬──────────────────────────────┘
               │ calls
               ▼
┌─────────────────────────────────────────────┐
│       Business Logic Layer                  │
│  ┌──────────────────────────────────────┐   │
│  │  Services (Interfaces + Impl)        │   │
│  │  • AuthService                       │   │
│  │  • CourseOfferingService             │   │
│  │  • RegistrationService               │   │
│  │  • StudentService                    │   │
│  │  • ScheduleService                   │   │
│  └──────────────────────────────────────┘   │
└──────────────┬──────────────────────────────┘
               │ calls
               ▼
┌─────────────────────────────────────────────┐
│       Data Access Layer                     │
│  ┌──────────────────────────────────────┐   │
│  │  Repositories                        │   │
│  │  • UserRepository                    │   │
│  │  • CourseOfferingRepository          │   │
│  │  • RegistrationRepository            │   │
│  │  • ScheduleRepository                │   │
│  │  • etc.                              │   │
│  └──────────────────────────────────────┘   │
└──────────────┬──────────────────────────────┘
               │ connects
               ▼
┌─────────────────────────────────────────────┐
│       Database Layer                        │
│  ┌──────────────────────────────────────┐   │
│  │  MySQL Database                      │   │
│  │  • users table                       │   │
│  │  • students table                    │   │
│  │  • courses table                     │   │
│  │  • course_offerings table            │   │
│  │  • registrations table               │   │
│  │  • schedules table                   │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

## 🔟 Deployment Diagram

```
┌─────────────────────────────────────────────────────────┐
│                  Client Machine                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  JavaFX Application (.jar)                        │  │
│  │  • Main.java (Entry Point)                        │  │
│  │  • Controllers                                    │  │
│  │  • Views (FXML/CSS)                              │  │
│  └────────────────┬────────────────────────────────┘   │
│                   │ JVM                                │
└─────────────────┼────────────────────────────────────┘
                  │ TCP/IP Connection
                  │ Port: 3306
                  ▼
┌─────────────────────────────────────────────────────────┐
│              Database Server                            │
│  ┌───────────────────────────────────────────────────┐  │
│  │  MySQL 8.0                                        │  │
│  │  • course_registration_db                         │  │
│  │  • Tables (users, courses, registrations, etc.)   │  │
│  │  • Connections: JDBC                             │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  Storage: Hard Disk / SSD                              │
│  • Database Files                                      │
│  • Binary Logs                                         │
│  • Configuration Files                                │
└─────────────────────────────────────────────────────────┘

Dependencies:
• Java Runtime Environment (JRE) 21+
• JavaFX Runtime 25+
• MySQL JDBC Driver 8.0.33+
```

---

## 1️⃣1️⃣ Activity Diagram - Student Registration Process

```
                              START
                               │
                               ▼
                    ┌──────────────────┐
                    │  Open App        │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │  Login Page      │
                    └────────┬─────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
                    ▼                 ▼
            ┌──────────────┐   ┌──────────────┐
            │  Credential  │   │  Credential  │
            │  Valid?      │   │  Invalid?    │
            └──────┬───────┘   └──────┬───────┘
                   │ YES              │ NO
                   ▼                  └──→ Show Error
                                          │
                                          └──→ Retry
            ┌──────────────────┐
            │ Load Dashboard   │
            └────────┬─────────┘
                     │
                     ▼
            ┌──────────────────┐
            │ Display Classes  │
            │ (Available)      │
            └────────┬─────────┘
                     │
                     ▼
            ┌──────────────────┐
            │ Click Register   │
            │ Button           │
            └────────┬─────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │ Check Duplicate        │
        │ Registration?          │
        └────┬─────────────┬─────┘
             │             │
          YES│             │NO
             │             │
             ▼             │
        ┌──────────┐       │
        │  Error:  │       │
        │ Duplicate│       │
        └──┬───────┘       │
           │               │
           └─ Retry◄──────┘
                   │
                   ▼
        ┌────────────────────────┐
        │ Check Schedule         │
        │ Conflict?              │
        └────┬─────────────┬─────┘
             │             │
          YES│             │NO
             │             │
             ▼             │
        ┌──────────┐       │
        │  Error:  │       │
        │ Conflict │       │
        └──┬───────┘       │
           │               │
           └─ Retry◄──────┘
                   │
                   ▼
        ┌────────────────────────┐
        │ Check Room Capacity?   │
        └────┬─────────────┬─────┘
             │             │
          NO │ YES         │
             │             │
             ▼             │
        ┌──────────┐       │
        │  Error:  │       │
        │ Full     │       │
        └──┬───────┘       │
           │               │
           └─ Retry◄──────┘
                   │
                   ▼
        ┌────────────────────────┐
        │ Save Registration      │
        │ to Database            │
        └────────┬───────────────┘
                 │
                 ▼
        ┌────────────────────────┐
        │ Show Success Message   │
        └────────┬───────────────┘
                 │
                 ▼
                END
```

---

## 1️⃣2️⃣ Use Case Diagram

```
                          ┌──────────────────────┐
                          │   Course Registration│
                          │      System          │
                          └──────────────────────┘
                                     △
                  ┌───────────────────┼───────────────────┐
                  │                   │                   │
                  ▼                   ▼                   ▼
            ┌─────────┐         ┌──────────┐        ┌─────────┐
            │ Student │         │  Admin   │        │ Faculty │
            └────┬────┘         └────┬─────┘        └────┬────┘
                 │                   │                   │
         ┌───────┼───────┐       ┌───┼────┬────────┐    │
         │       │       │       │   │    │        │    │
         ▼       ▼       ▼       ▼   ▼    ▼        ▼    ▼
      ┌─────┐ ┌──────┐ ┌────┐ ┌────────────┐ ┌────────┐
      │Login│ │View  │ │View│ │Create      │ │View    │
      │     │ │Class │ │    │ │CourseOffng │ │Reports │
      └──┬──┘ └──┬───┘ │My  │ └────────────┘ └────────┘
         │       │     │Reg │     │
      ┌──┴──┐  ┌─┴──┐  └──┬─┘  ┌──┴──┐
      │     │  │    │     │    │     │
      ▼     ▼  ▼    ▼     ▼    ▼     ▼
   ┌──────────────┐ ┌──────────────────┐
   │ Register for │ │ View/Edit Schedule
   │ Course       │ │ Detect Conflicts  │
   └──────┬───────┘ └──────────────────┘
          │
      ┌───┴────────────────┐
      │                    │
      ▼                    ▼
  ┌────────────────┐  ┌────────────────┐
  │  <<include>>   │  │  <<include>>   │
  │ Check Duplicate│  │ Check Conflict │
  └────────────────┘  └────────────────┘
```

---

## 1️⃣3️⃣ ER Diagram (Database)

```
┌─────────────┐
│   users     │
├─────────────┤
│ userId (PK) │◄──┐
│ username    │   │
│ email       │   │ (1:1)
│ password    │   │
│ fullName    │   │
│ role        │   │
└─────────────┘   │
       △          │
       │          │
    (1:1)         │
       │          │
  ┌────┴────────┐ │
  │             │ │
┌─────────────┐ │ ┌──────────────┐
│  students   │ │ │   admin      │
├─────────────┤ │ ├──────────────┤
│ studentId   │ │ │ adminId      │
│ userId(FK)──┼─┴─│(FK)userId    │
│ className   │   │ department   │
│ majorId(FK)─┼───┼──┐           │
│ facultyId(FK)   │  │           │
└─────────────┘   └──┴───────────┘
       │
       │ (1:*)
       │
   ┌───┴──────────┐
   │              │
┌──────────────┐ ┌──────────────────────┐
│   majors     │ │  registrations       │
├──────────────┤ ├──────────────────────┤
│ majorId (PK) │ │ registrationId (PK)  │
│ majorName    │ │ studentId (FK)       │
│ facultyId(FK)│ │ courseOfferingId(FK) │
│ credits      │ │ registrationDate     │
└──────────────┘ │ status               │
       │         │ grade                │
       │         └──────────────────────┘
       │                │
   (1:*)           (1:*)│
       │                │
    ┌──┴────────┐   ┌───┴────────────────┐
    │           │   │                    │
┌─────────────┐┌───────────┐    ┌─────────────┐
│  faculties  ││  courses  │    │ CourseOffng │
├─────────────┤├───────────┤    ├─────────────┤
│ facultyId(P)││courseId(P)│    │ offeringId  │
│ facName     ││courseName ││    │ courseId(FK)│
│ description ││courseCode ││    │ semesterId  │
└─────────────┘│credits    │    │ facultyId   │
       △       │facultyId  │    │ roomId      │
       │       │(FK)       │    │ maxCap      │
    (1:*)      └───────────┘    │ currCap     │
       │            │           └─────────────┘
       │         (1:*)                │
       │            │           ┌─────┴─────────────┐
       └────────────┼───────────┤                   │
                    │       ┌───────────────────┐   │
                    │       │Offering_Schedule  │   │
                    │       ├───────────────────┤   │
                    │       │ id (PK)           │   │
                    │       │ offeringId (FK)──────┐
                    │       │ scheduleId (FK)───────┼─┐
                    │       │ startDate        │   │ │
                    │       │ endDate          │   │ │
                    │       └───────────────────┘   │ │
                    │                               │ │
         ┌──────────┴────────────┐       ┌──────────┼─┼───┐
         │                       │       │          │ │   │
    ┌──────────┐            ┌──────────┐│   ┌──────────────┐
    │ semesters│            │  rooms   ││   │  schedules   │
    ├──────────┤            ├──────────┤│   ├──────────────┤
    │semester (P)           │ roomId(P)├┐  │ scheduleId(P)│
    │ semName  │            │ roomName ││  │ dayOfWeek    │
    │ acadYear │            │ building ││  │ startTime    │
    │ startDate│            │ capacity ││  │ endTime      │
    │ endDate  │            │ hasProj  ││  └──────────────┘
    │ registS  │            │ hasAC    │└─ (1:*)
    │ registEnd            └──────────┘
    └──────────┘
```

---

## Legend & Notations

### UML Notations Used

```
┌─────────────────────────────┐
│ ClassName                   │
├─────────────────────────────┤
│ - privateField: Type        │  (-)  = Private
│ + publicMethod(): Type      │  (+)  = Public
│ # protectedField: Type      │  (#)  = Protected
│ ~ packageField: Type        │  (~)  = Package
├─────────────────────────────┤
│ Relationships:              │
│ △ Inheritance               │
│ ◇ Composition               │
│ ○ Aggregation               │
└─────────────────────────────┘

Cardinality Notation:
│ 1 : 1     (One to One)
│ 1 : *     (One to Many)
│ * : *     (Many to Many)

Association Types:
→ uses/calls
← implements/extends
◇ composes
○ aggregates
```

---

## Key Points Illustrated

### 1. Architecture Layers
- **Presentation**: Controllers + Views (JavaFX)
- **Business Logic**: Services (Validation, Business Rules)
- **Data Access**: Repositories (JDBC Queries)
- **Database**: MySQL (Persistence)

### 2. Critical Features
- ⭐ **Schedule Conflict Detection**: Implemented in `CourseOfferingRepository` & `Service`
- ⭐ **Duplicate Registration Prevention**: Implemented in `RegistrationServiceImpl`
- ⭐ **Controller Schedule Loading**: Fixed in `CreateCourseOfferingFormController`

### 3. Design Patterns
- **MVC**: Model-View-Controller
- **Repository**: Data Access Abstraction
- **Service Layer**: Business Logic Separation
- **Singleton**: Database Connection Management
- **Factory**: Entity Creation

### 4. Key Relationships
- User (1) → (1) Student/Admin
- Course (1) → (*) CourseOffering
- CourseOffering (1) → (*) Registration
- Student (*) → (*) CourseOffering (via Registration)
- CourseOffering (*) → (*) Schedule (via CourseOfferingSchedule)

---

**Last Updated:** November 23, 2025  
**Format:** PlantUML-compatible Markdown  
**Scope:** Complete System Architecture
